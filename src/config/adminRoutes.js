// Deliberately not linked anywhere in the public UI (no navbar/footer entry).
// Change this to your own secret value before deploying — anyone who knows
// this string can reach the admin login wall (though not the dashboard itself,
// since it still requires real credentials + email OTP 2FA behind it).
export const ADMIN_BASE_PATH = "/internal-ops-7f3k2x";
